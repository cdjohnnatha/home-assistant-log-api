# Spring Boot Configuration
SPRING_PROFILE_ACTIVE=prod

# Server Configuration  
SERVER_PORT=8080

# JVM Configuration
JAVA_OPTS=-Xmx512m -Xms256m

# Logging Configuration
LOGGING_LEVEL_COM_HOMEASSISTANT=INFO
LOGGING_LEVEL_ROOT=WARN

# Application Name
SPRING_APPLICATION_NAME=logs-api

# AWS Configuration
# Get these values from AWS IAM Console:
# 1. Create IAM user for your application
# 2. Generate Access Keys in Security Credentials tab
# 3. Create/use existing SNS Topic and copy its ARN
AWS_ACCESS_KEY=AWS_ACCESS_KEY_EXAMPLE
AWS_SECRET_KEY=AWS_SECRET_KEY_EXAMPLE
AWS_REGION=AWS_REGION_EXAMPLE
AWS_SNS_TOPIC_ARN=AWS_SNS_TOPIC_ARN_EXAMPLE

# Application specific configuration
LOGS_API_PORT=8080
WARN_LOG_LEVEL=INFO

# Optional Database Configuration (uncomment if needed)
# DATABASE_URL=jdbc:postgresql://localhost:5432/logs_db
# DATABASE_USERNAME=logs_user  
# DATABASE_PASSWORD=your_password_here

# ==================================================
# SETUP INSTRUCTIONS:
# ==================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace AWS_ACCESS_KEY with your real AWS Access Key
# 3. Replace AWS_SECRET_KEY with your real AWS Secret Key  
# 4. Replace AWS_SNS_TOPIC_ARN with your real SNS Topic ARN
# 5. Optionally adjust other values as needed
# 6. Never commit .env to version control - it contains secrets!
